# Core retrieval (src/retrieval.py, used by the CLI demo and the API alike):
rank_bm25>=0.2.2
# API layer (src/api.py):
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Optional, only needed if you set LLM_PROVIDER=anthropic:
anthropic>=0.34.0
